# 🔧 Исправление ошибки 404 на Netlify

## Проблема
Если вы видите "Page not found" на Netlify, это обычно означает неправильные настройки деплоя.

## Решение

### Шаг 1: Проверьте настройки в Netlify

1. Откройте ваш сайт на [app.netlify.com](https://app.netlify.com)
2. Выберите ваш сайт `chimerical-tarsier-adc97b`
3. Перейдите в **Site settings** → **Build & deploy**
4. В разделе **"Build settings"** нажмите **"Edit settings"**
5. Убедитесь, что указано:
   - **Base directory:** (оставьте пустым)
   - **Build command:** (оставьте пустым или удалите)
   - **Publish directory:** `public`
6. Нажмите **"Save"**

### Шаг 2: Закоммитьте изменения и запушьте

```bash
git add .
git commit -m "Fix Netlify deployment configuration"
git push
```

### Шаг 3: Пересоберите сайт в Netlify

1. В Netlify перейдите в **Deploys**
2. Нажмите **"Trigger deploy"** → **"Clear cache and deploy site"**

### Шаг 4: Проверьте результат

Через 1-2 минуты сайт должен заработать по адресу:
`https://chimerical-tarsier-adc97b.netlify.app`

## Альтернатива: Ручной деплой через Netlify CLI

Если проблемы продолжаются, можно задеплоить вручную:

```bash
# Установите Netlify CLI (если еще не установлен)
npm install -g netlify-cli

# Войдите в Netlify
netlify login

# Задеплойте из папки public
cd public
netlify deploy --prod
```

## Проверка структуры файлов

Убедитесь, что в папке `public/` есть:
- ✅ `index.html`
- ✅ `app.js`
- ✅ `styles.css`
- ✅ `data/news.json`
- ✅ `_redirects`

Все эти файлы должны быть закоммичены в Git.

