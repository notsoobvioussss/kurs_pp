# O&G Threat Lens

Радар инцидентов и киберугроз для нефтегазовой отрасли.

## Структура проекта

- `public/` - статические файлы сайта (HTML, CSS, JS)
- `data/` - данные новостей (JSON)
- `scraper/` - скрипт для парсинга новостей

## 🚀 Развертывание (бесплатные варианты)

### Вариант 1: Netlify (⭐ Рекомендуется - самый простой)

**Бесплатно для приватных репозиториев!**

1. Зарегистрируйтесь на [netlify.com](https://netlify.com) (через GitHub)
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите ваш репозиторий `kurs_pp`
4. В настройках:
   - **Base directory:** оставьте пустым
   - **Publish directory:** `public`
   - **Build command:** оставьте пустым
5. Нажмите **"Deploy site"**
6. Готово! Сайт будет доступен по адресу типа `https://ваш-сайт.netlify.app`

**Автоматический деплой:** Каждый push в `main` автоматически обновит сайт.

---

### Вариант 2: Vercel (быстрый и надежный)

**Бесплатно для приватных репозиториев!**

1. Зарегистрируйтесь на [vercel.com](https://vercel.com) (через GitHub)
2. Нажмите **"Add New Project"**
3. Выберите репозиторий `kurs_pp`
4. В настройках:
   - **Framework Preset:** Other
   - **Root Directory:** `./public`
5. Нажмите **"Deploy"**
6. Готово! Сайт будет доступен по адресу типа `https://ваш-сайт.vercel.app`

---

### Вариант 3: Cloudflare Pages

**Бесплатно для приватных репозиториев!**

1. Зарегистрируйтесь на [pages.cloudflare.com](https://pages.cloudflare.com)
2. Подключите GitHub аккаунт
3. Выберите репозиторий `kurs_pp`
4. В настройках:
   - **Framework preset:** None
   - **Build command:** (оставьте пустым)
   - **Build output directory:** `public`
5. Нажмите **"Save and Deploy"**

---

### Вариант 4: Render

1. Зарегистрируйтесь на [render.com](https://render.com) (через GitHub)
2. Создайте новый **Static Site**
3. Подключите репозиторий `kurs_pp`
4. В настройках:
   - **Build Command:** (оставьте пустым)
   - **Publish Directory:** `public`
5. Нажмите **"Create Static Site"**

---

### Вариант 5: GitHub Pages (только для публичных репозиториев)

Если сделаете репозиторий публичным, можно использовать GitHub Pages:

1. Перейдите в **Settings** → **Pages**
2. В разделе **"Source"** выберите **"GitHub Actions"**
3. После push сайт будет доступен по адресу:
   `https://notsoobvioussss.github.io/kurs_pp/`

### Обновление данных

Для обновления данных новостей:
1. Запустите скрипт парсера: `python scraper/fetch_news.py`
2. Закоммитьте изменения: 
   ```bash
   git add public/data/news.json
   git commit -m "Update news"
   git push
   ```
3. Сайт автоматически обновится на всех платформах (Netlify, Vercel и т.д.)

## Локальная разработка

Для локального запуска используйте любой HTTP-сервер:

```bash
# Python 3
cd public
python -m http.server 8000

# Node.js (если установлен)
npx serve public
```

Затем откройте в браузере: `http://localhost:8000`

